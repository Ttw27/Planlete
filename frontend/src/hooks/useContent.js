// Re-export from the main lib version
// This allows Landing.jsx to import: import { useContent } from "../hooks/useContent"
export { useContent, useAllContent, notifyContentChanged } from "../lib/useContent";
